-- WARNING: This only works if the game has a broken/unsecured RemoteEvent
local targetName = "PlayerNameHere"
local remote = game:GetService("ReplicatedStorage"):WaitForChild("KickRemote") -- Example name

remote:FireServer(game.Players[targetName])
